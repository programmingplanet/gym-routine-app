# API Configuration
# Set USE_API to 'true' to use the backend API instead of local storage
NEXT_PUBLIC_USE_API=false
# Backend API URL (default: http://localhost:3001)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Node Environment
NODE_ENV=production
