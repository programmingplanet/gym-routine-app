# API Configuration
# Set USE_API to 'true' to use the backend API instead of local storage
NEXT_PUBLIC_USE_API=false

# Backend Private URL (server-side only, used by Next.js API routes to call FastAPI)
# This is the internal Docker service name or localhost for development
BACKEND_PRIVATE_URL=http://gym-routine-api:8000

# Node Environment
NODE_ENV=production
